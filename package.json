{
  "name": "share",
  "version": "0.1.0",
  "repository": "git@github.com:GenerousLabs/share.git",
  "author": "Generous Labs",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "husky": {
    "hooks": {
      "pre-commit": "cd expo && yarn lint && cd ../server && yarn lint && cd ../website && yarn lint"
    }
  },
  "devDependencies": {
    "husky": "^4.3.5"
  }
}
